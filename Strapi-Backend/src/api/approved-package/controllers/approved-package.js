'use strict';

/**
 * approved-package controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::approved-package.approved-package');
